{
  "name": "open-video-automation-framework",
  "version": "0.0.0",
  "private": true,
  "description": "Specification-first, AI-agnostic framework for deterministic vertical video automation.",
  "type": "module",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "acceptance": "npm run check && npm run benchmark:render && npm audit",
    "benchmark:render": "node scripts/benchmark-render.mjs",
    "check": "npm run check:workspace && npm run typecheck && npm test",
    "check:workspace": "node scripts/check-workspace.mjs",
    "db:migrate": "node packages/persistence/dist/cli/migrate.js",
    "test:integration": "vitest run tests/integration",
    "typecheck": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  }
}
