# Examples

Example projects and scripts live here as acceptance fixtures.

- `basic-vertical-short.json` is the Phase 15 smoke-test project. It exercises JSON Script validation, Timeline build, media planning, scene HTML generation and FFmpeg command construction without requiring real browser or FFmpeg execution.
