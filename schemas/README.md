# Schemas

This directory is reserved as the source of truth for JSON Schemas.

Phase 03 defines the first JSON Script schema in `json-script.schema.json`.

Phase 13 defines the Style Profile schema in `style-profile.schema.json`. Reference analysis must produce a Style Profile, not direct renderer instructions.
