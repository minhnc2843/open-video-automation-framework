# Schemas

This directory is reserved as the source of truth for JSON Schemas.

Phase 00 does not define the JSON Script schema yet. Script schema work begins in Phase 03 unless an earlier approved task changes that order.
